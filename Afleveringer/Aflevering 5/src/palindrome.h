
/**
 * Determine whether str is a palindrome (spelled the same forwards and backwards) using an iterative algorithm.
 * @param str
 * @return
 */
int is_palindrome_iter(const char *str);

/**
 * Determine whether str is a palindrome (spelled the same forwards and backwards) using a recursive algorithm.
 * @param str
 * @return
 */
int is_palindrome_rec(const char *str);
